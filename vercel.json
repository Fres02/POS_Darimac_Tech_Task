{
  "framework": null,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @pos/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
